---
import Image from "astro/components/Image.astro";
import style from "../../styles/hero.module.css";
---

<section class="relative bg-white text-black overflow-hidden">
  <div class={style.leaves}>
    <div></div>
    <div></div>
    <div></div>
    <div></div>
    <div></div>
    <div></div>
    <div></div>
  </div>

  <div
    class="container mx-auto px-4 py-16 md:py-24 flex flex-col md:flex-row items-center"
  >
    <div class={`md:w-1/2 z-10 overflow-hidden`}>
      <h1
        class={`${style.fadeInUpText} text-4xl md:text-5xl lg:text-6xl font-bold mb-4 text-black`}
      >
        Gestión para Cafetines Universitarios
      </h1>
      <p
        class={`${style.fadeInUpTextDelay} text-lg md:text-xl mb-8 text-gray-600`}
      >
        Optimiza la operación de tus cafeterías universitarias con nuestro
        sistema integral de gestión. Mejora la experiencia de tus estudiantes y
        aumenta la eficiencia.
      </p>
      <div class={`flex flex-col sm:flex-row gap-4 ${style.fadeInUpDelay}`}>
        <button
          aria-label="Button demo"
          class="bg-black text-white rounded-lg hover:bg-gray-900 text-lg px-8 py-2 cursor-pointer"
        >
          Solicitar Demo
        </button>
        <button
          aria-label="Know more"
          class="border-black border rounded-lg text-black hover:bg-gray-100 text-lg px-8 py-2 cursor-pointer"
        >
          Conocer Más
        </button>
      </div>
    </div>

    <div
      class={`md:w-1/2 mt-12 md:mt-0 flex justify-center ${style.fadeInUpDelay}`}
    >
      <div class="relative w-64 h-64 md:w-80 md:h-80">
        <Image
          src="/mikhuy_logo.webp"
          alt="Mikhuy Mascot"
          width="320"
          height="320"
          class="object-contain drop-shadow-xl w-full h-full"
          loading="lazy"
        />
      </div>
    </div>
  </div>
</section>
